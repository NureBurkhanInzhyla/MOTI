<Window x:Class="Lab4MOTI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Game Battle" Height="400" Width="500">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="GameGrid"
          Grid.Row="1"
          AutoGenerateColumns="True"
          SelectionUnit="Cell"
          HeadersVisibility="All"
          IsReadOnly="True"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Атака" Width="100" Margin="5" Click="Attack_Click"/>
            <Button Content="Елементальний скіл" Width="100" Margin="5" Click="Skill_Click"/>
            <Button Content="Ухилення" Width="100" Margin="5" Click="Dodge_Click"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Оптимальна стратегія"
                Width="200" Margin="10"
                HorizontalAlignment="Center"
                Click="Optimal_Click"/>

        <TextBlock x:Name="ResultText"
                   Grid.Row="0"
                   FontSize="14"
                   TextAlignment="Center"
                   Margin="5"/>
    </Grid>
</Window>